Interview the researcher to create a project.yaml for MicroAgent. Ask ONE question at a time. Gather: organism/sample type, microscopy modality (fluorescence/brightfield/phase/EM/H&E), target structures (nuclei/cells/organelles/custom), channel configuration, image format and approximate dimensions, whether ground truth masks exist, analysis end goal, and compute environment (local GPU model if known). After all answers, generate project.yaml and explain what model/parameters were auto-selected and why.
